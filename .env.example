DATABASE_URL=postgres://paperclip:paperclip@localhost:5432/paperclip
PORT=3100
SERVE_UI=false
BETTER_AUTH_SECRET=paperclip-dev-secret

# @finapticoos/memory — Supabase Finaptico (project ref utwhvnafvtardndgkbjn).
# Direct URL used for DDL/migrations + low-volume runtime; pooler URL for
# high-concurrency prod workloads (TODO: swap once Bloque 6 deploys to Easypanel).
# FINAPTICOOS_MEMORY_DATABASE_URL=postgresql://postgres:PASSWORD@db.utwhvnafvtardndgkbjn.supabase.co:5432/postgres
# OPENAI_API_KEY=sk-proj-... (reuse CRM M8 RAG embeddings key — do not open a new account)

# @finapticoos/adapter-anthropic-api — Claude Sonnet 4.6 / Haiku 4.5 with prompt caching.
# Reuse the Finaptico stack key (same one used for blog posts via n8n).
# ANTHROPIC_API_KEY=sk-ant-api03-...

# Discord webhook for daily merge digest (scripts/discord-daily-digest.sh)
# DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/...
