name: Auto Deploy Easypanel

on:
  push:
    branches: [finaptico/main]

jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Easypanel deploy
        run: curl -fsSL -X POST "${{ secrets.EASYPANEL_DEPLOY_HOOK }}"
